// import PropTypes from 'prop-types';

const PrivateRoutes = () => {
    return (
        <div>
            
        </div>
    );
};

// PrivateRoutes.propTypes = {
    
// };

export default PrivateRoutes;